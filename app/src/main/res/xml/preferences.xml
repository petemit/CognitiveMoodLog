<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:defaultValue="false"
        android:key="@string/password_protect_key"
        android:summary="Encrypt and protect your log entries with a password"
        android:title="Protect Log Entries" />
    <Preference
        android:key="@string/drive_backup_key"
        android:summary="Backup current Log Entries using your Google Drive account (needs Contacts Permissions)"
        android:title="Backup Log Entries" />
    <Preference
        android:key="@string/backup_to_csv_key"
        android:summary="Download current Log Entries into a CSV"
        android:title="Download Log Entries" />
    <Preference
        android:key="restore_from_backup"
        android:summary="Restore from Google Drive Backup if you've created it previously"
        android:title="Restore from Backup"
        />
    <Preference
        android:key="delete_files"
        android:summary="Delete any Backups made to your Google Drive"
        android:title="Delete Backed Up DB"
        />
</PreferenceScreen>